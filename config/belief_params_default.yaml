/**:
  ros__parameters:
    frame_id: map

    x_min: 0.0
    x_max: 2.0
    y_min: 0.0
    y_max: 4.0

    kernel_type: rbf
    training_iter: 25
    learning_rate: 0.1

    init_lengthscale_x: 0.2
    init_lengthscale_y: 0.2
    init_outputscale: 1.0
    init_noise: 0.01

    refit_policy: every_measurement
    refit_every_k: 1

    publish_visualization: true
    visualization_grid_step: 0.1
    visualization_z_mode: flat
    visualization_height_scale: 0.5

    # visualization only
    field_color_min: 0.0
    field_color_max: 0.015
    use_fixed_field_color_max: false
    z_offset: -0.8